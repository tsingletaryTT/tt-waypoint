__pycache__/
*.pyc
*.egg-info/
.venv/
generated/

# Captured reference activations: large binary tensors, fully reproducible via the
# capture_*.py scripts (which ARE tracked) against the real HF checkpoint. One already
# hit 97MB, close to GitHub's 100MB hard block -- don't let these bloat repo history.
ref_activations/*.pt
