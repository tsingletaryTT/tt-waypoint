# Waypoint-1.5-1B TTNN bring-up — time/token/hardware log

Tracking wall-clock time, approximate token usage, and hardware configuration for the
Claude-driven bring-up of [Overworld/Waypoint-1.5-1B](https://huggingface.co/Overworld/Waypoint-1.5-1B)
on Tenstorrent Blackhole, per Taylor's explicit request to measure "how long it takes,
how many tokens it burns, and which hardware configuration."

## Hardware

**P300×2** (2× P300c boards, 4 chips total). This is the ONLY configuration available on
this box — confirmed via `tt-smi -ls`: no P150 boards are physically present, so
"P150x4" was never actually an open choice here. Can be opened as a (1,4) line mesh or a
(2,2) "QB2" mesh depending on what the model needs (see tt-skyreels' bring-up for why
that distinction matters on this specific box).

## Token accounting method (caveated)

This session shows a `total_tokens` remaining counter in the low-level system context.
I'm using it as `tokens_burned ≈ baseline_remaining − current_remaining` at each
checkpoint. This is an approximation, not a precise ledger: it can be affected by context
compaction/caching behavior outside my control, and it covers only tokens spent *in this
conversation* — not any separate tool-internal cost. Treated as directionally accurate,
not exact.

## Scoping phase (before this log existed — reconstructed from the conversation)

Not counted in the totals below (those start at the bring-up commit point), but for
context: architecture research (reading `transformer/model.py`, `vae/ae_model.py`,
`modular_blocks.py`, loading the reference pipeline components on CPU) took roughly one
conversation segment before Taylor said "I want to try."

## Bring-up phase

| Checkpoint | Wall clock (UTC) | `total_tokens` remaining | Notes |
|---|---|---|---|
| **Start** | 2026-09-09T19:58:39Z | 14,995,739 | Repo created, hardware confirmed (P300×2 only), this log started. |
| Reference model runs | 2026-09-09T20:11:44Z | 14,976,426 | **Milestone**: got the actual upstream reference PyTorch pipeline (CPU, no TT hardware) running end to end — seeded a world from an image, then generated 2 more controller-conditioned frames, no crashes. Hit and fixed one real bug of my own along the way (forced `dtype=torch.float32`, which the pipeline's internal VAE-decode step doesn't support — matched the README's `bfloat16` instead and it worked). Confirmed exact tensor contracts: `prompt_embeds` [1,512,2048], `button_tensor` [1,1,256], `mouse_tensor` [1,1,2], `scroll_tensor` [1,1,1], `scheduler_sigmas` [5], `frame_timestamp` [1,1], plus the `StaticKVCache` object shape. Elapsed: 13m5s; ≈19,313 tokens by the approximation above. Each single-frame CPU generation call itself takes ~8-10 minutes wall clock (unaccelerated `flex_attention` eager fallback, per its own warning) — a real, separate cost from my own working time, and a preview of how expensive *correctness-checking against this reference* will be at every TTNN porting step. |

| Sample frame captured, plan written | 2026-09-09T20:16:32Z | 14,959,743 | Saved and sent an actual generated frame (`seed_frame.png`) as tangible proof the reference pipeline works. Wrote `PORT_PLAN.md` (6 staged phases) and confirmed by reading real code that Stage 1 (UMT5-XL text encoder) is checkpoint-agnostic reuse from `models.tt_dit.pipelines.wan.text_encoder.TextEncoder` — no architecture guessing, verified against the actual source. Elapsed since start: 17m53s. |

| **Stage 1 hardware-verified** | 2026-09-09T20:20:50Z | 14,942,273 | **Milestone**: UMT5-XL text encoder running on REAL Blackhole hardware (P300×2 board, 1×1 mesh, one chip leased via gozer). Hit one real bug (`ftfy` missing from the shared `.tenstorrent-venv` — same package tt-skyreels' bring-up hit independently, now confirmed to also be a gap in tt-metal's own WAN text encoder module, not just SkyReels' port). Fixed, then: constructed `models.tt_dit.encoders.umt5.UMT5Encoder` directly (bypassing tt_dit's WAN-specific `TextEncoder` wrapper, which assumes a subfolder layout `google/umt5-xl` doesn't have), loaded `UMT5EncoderModel.from_pretrained("google/umt5-xl").state_dict()` into it — **zero missing, zero unexpected keys** — and ran a real forward pass on the chip. Output shape matched the torch reference exactly ([1,512,2048]); mean abs diff 0.0045 (expected bf16 noise, not a bug). Elapsed since start: 22m11s. |

| **Stage 2 hardware-verified** | 2026-09-09T20:49:28Z | 14,956,882 | **Milestone**: patchify (Conv2d-as-unfold-matmul, [1,32,32,64]→[1,2048,16,32]) and AdaLN/out_norm (weight-free RMSNorm + SiLU-conditioned scale/bias) both verified on real Blackhole hardware against captured reference activations (`ref_activations/block_activations.pt` — captured once via PyTorch forward hooks, so later stages don't need to re-run the ~10min CPU reference each time). Confirmed the exact latent geometry: VAE outputs a 32×64×32ch latent per frame, patchify(2,2) → 16×32=512 tokens, matching `tokens_per_frame`. Real bug-fixing along the way: the shared `.tenstorrent-venv` lacked `tensordict`/`einops` (needed `trust_remote_code`, now installed) and its `diffusers` predates `AutoModel`/`modular_pipelines` entirely — worked around by loading the transformer's weights directly via `safetensors.torch.load_file` rather than touching diffusers at all for hardware-side checks. Both diffs: mean abs ≈0.0023 (expected bf16 noise). Elapsed since start: 51m. |

| **Stage 3 hardware-verified** | 2026-09-09T20:51:32Z | 14,950,850 | **Milestone**: `NoiseConditioner` (Fourier features → MLP; mean abs diff 0.0093, bf16 noise) and `ControllerInputEmbedding` (plain MLP; **exact match, 0.0 diff**) both verified on real hardware. Stages 2-3 (patchify, unpatchify's inverse still pending explicit check but same op family as patchify, AdaLN, both conditioning embeddings) are now done. Remaining before a full frame: `CondHead`/`CrossAttention`/`MLPFusion` (straightforward, same linear-algebra pattern as everything so far) and then **Stage 4: the real risk** — local/global block-sparse causal attention with the per-layer dilated ring-buffer KV cache. Elapsed since start: 53m. |

| **Stage 4 first pass: real finding, hardware-verified** | 2026-09-09T21:09:51Z | 14,886,832 | **Milestone, and a genuine correction to my own hypothesis.** Set out to test "attention reduces to plain self-attention over the current frame for an empty cache" — this was WRONG, caught by verification, not assumed. The actual bug hunt (documented in full below) revealed the real behavior: the reference runs **dense (unmasked) attention over the ENTIRE per-layer capacity buffer** (8704 positions for a local layer: 16-frame ring + 1-frame tail), where never-written slots are zero and contribute their (small, predictable) softmax mass rather than being excluded. My first attempt — masking to only the "written" tail — was mathematically reasonable from reading the code but empirically wrong (mean abs diff 0.093, correlation 0.94 — a real bug, not bf16 noise). Verified the correct behavior first in pure PyTorch (mean abs diff 0.00046 using the real captured q/k/v) then on real Blackhole hardware (mean abs diff 0.018 — larger than shorter-sequence stages, plausibly genuine bf16 accumulation error over an 8704-long softmax vs. 512; flagged for revisit, not dismissed as noise). **Practical implication: Stage 4 does not need any block-sparse masking or gather logic at all for a first correctness pass** — just replicate the reference's exact zero-initialized ring-buffer layout and run plain dense attention over the whole buffer every time. This meaningfully de-risks the stage I scoped as "the hard part." Elapsed since start: 1h11m. |

### How the Stage 4 bug was actually found (for anyone continuing this)

1. First attempt used a captured activation that turned out to be from **frame index 2** (the pipeline pre-seeds multiple frames from the starting image before real generation begins), not frame 0 — invalidating the "empty cache" assumption before I'd even gotten to the real bug. Caught by checking `pos_ids["f_pos"].unique()` directly rather than trusting which call a "fire-once" hook happened to catch.
2. Fixed by calling `WorldModel.forward` **directly** with a synthetic frame 0 and a freshly-constructed `StaticKVCache` — full control, full visibility, and much faster than running the real pipeline (seconds vs. ~10 minutes).
3. Still 0.093 mean diff. Rather than keep guessing at the mask semantics, **monkeypatched `flex_attention` itself** to capture its exact `(q, k, v, block_mask)` arguments — ground truth, zero reimplementation risk.
4. Tested the real captured `q, k, v` two ways: sliced to just the "written" tail (0.094 mean — still wrong) vs. the full untouched capacity buffer (0.00046 mean — correct). That comparison is what actually located the bug.

| **Switched onto the actual ttm-* bring-up skills** | 2026-09-09T21:35:20Z | 14,944,153 | Taylor asked directly whether I was using the agentic bring-up skills tt-model-manager's own AGENTS.md points to (the `tenstorrent/skills` marketplace, `ttm-*` skills already loaded in this session) — I hadn't been; I'd been improvising ad hoc capture/debug scripts. Invoked `ttm-functional-decoder` properly. Learned its real file contract: `models/autoports/<model>/tt/functional_decoder.py`, a `LightweightModule` subclass with `from_state_dict`/`prefill_forward`/`decode_forward`, PCC>=0.995 as the correctness bar. Wrote `waypoint_ttnn/tt/functional_decoder.py` assembling everything validated so far (Stages 1-4) into ONE real `WorldDiTBlock` implementation — cross-attention correctly omitted entirely (`prompt_conditioning: null` in the real downloaded config, confirmed by reading the value, not assumed), `ctrl_conditioning_period=3` fusion included. First ran it inside tt-metal's own `models/autoports/` tree per the skill's literal contract, verified it there (**PCC 0.999** against the real HF reference for layer 0's frame-0 prefill — exceeds the skill's own 0.995 bar), then — per Taylor's explicit request to keep using our own dedicated repo, and to codify this as a repeatable pattern — migrated the file into `waypoint_ttnn/tt/` here, re-verified the migration produced an identical result, and deleted the tt-metal-tree copy. Documented the "bring-up methodology from ttm-*, repo location from the packaging convention" split explicitly in CLAUDE.md and as a memory ([[agentic-bringup-plus-decoupled-repo]]) so it's discoverable next time rather than re-derived. One remaining honest caveat: the PCC check's absolute error (mean 0.11, one outlier position at max 14.7) is larger than earlier stages' — traced to a single position under synthetic RANDOM inputs (not realistic activations), consistent with bf16 rounding sensitivity at the tail rather than a structural bug, but not yet independently re-confirmed with a second random seed. Elapsed since start: 1h37m. |

| **Full-model assembly: two real bugs found and fixed** | 2026-09-09T21:58:00Z | 14,675,593 | **Milestone.** `test_full_model.py` (24-layer `WaypointWorldModel`) started at correlation **0.052** — essentially uncorrelated with the reference. Root-caused and fixed two independent, real bugs rather than accepting the first plausible-looking explanation: (1) **`x_input` was never actually reproducible.** `capture_synthetic_frame0.py` imports `diffusers.AutoModel`, which does not exist in the shared `.tenstorrent-venv` (confirmed: `diffusers==0.32.1` there predates `AutoModel` entirely — same gap Stage 2 hit). The saved `ref_activations/synthetic_frame0.pt` must have been captured in a different venv/torch build (found one: `tt-skyreels/.venv`, `diffusers==0.38.0`). Test scripts were reconstructing `x` via `torch.manual_seed(0); torch.randn(...)`, assuming that reproduces the captured tensor bit-for-bit — verified directly that it does NOT (max diff 3.27, first 5 elements completely different): bf16 RNG draws for a given seed are not portable across torch builds/backends. Fix: use the actually-saved `ref["x_input"]` everywhere instead of reconstructing it — the capture script already saves it for exactly this reason. This alone took correlation from 0.052 → 0.943. (2) **Value-residual `v1` was being reassigned instead of pinned.** Reference `Attn.forward`: `v1 = v if v1 is None else v1; v = lerp(v, v1, lamb); return y, v1` — v1 is fixed at whatever layer FIRST produced it (layer 0, where it enters `None`) and threaded **unchanged** through every later layer's lerp. `functional_decoder.py`'s `_attn` was returning the freshly-lerped `v` as `v1_out` on every call, so the value-residual signal drifted layer-over-layer instead of staying pinned to the origin. Invisible in the layer-0-only test (v1 enters as `None` there either way, so behavior is identical to correct on that one layer) — a real, compounding bug from layer 1 onward. Fixed by pinning `v1` on first assignment and returning it unchanged. Took correlation 0.943 → 0.948. Elapsed since start: 2h. |

| **Full-model residual gap: localized and explained, not a bug** | 2026-09-09T22:05:00Z | 14,675,593 | Extended the capture to hook all 24 blocks (`capture_all_blocks.py`, via the `tt-skyreels` venv) and compared our own per-layer outputs against the reference's at every layer. Found smooth, gradual, monotonic-ish growth in absolute error across ALL 24 layers (mean abs diff 0.11 at layer 0 → 3.8 at layer 23) with **no discontinuity** at any specific layer index and no difference in behavior between local (`is_global=False`) and global/dilated (`is_global=True`, every 4th layer) layer types — the signature of ordinary compounding numerical drift through a deep residual stream, not a localized logic defect. Tested this directly rather than asserting it: ran the MLP path in ttnn float32 instead of bf16 as a diagnostic (SDPA itself only accepts bf16/bf8/bf4, so attention couldn't be tested this way) — correlation barely moved (0.948 → 0.951), ruling out "our own bf16 casting is imprecise" as the explanation. Conclusion: the residual gap between the single-layer PCC (0.999, meets the `ttm-functional-decoder` skill's 0.995 bar) and the 24-layer full-model correlation (0.948) is consistent with independent per-op bf16 divergence between Blackhole TTNN kernels and the reference's CPU-eager bf16 PyTorch ops compounding across 24 sequential residual layers — an expected characteristic of a full-bf16 hardware path at this depth, not a remaining correctness bug. `test_full_model.py`'s `assert corr > 0.99` is left failing and undiluted (0.948) rather than weakened, as honest evidence of exactly where this stands. Elapsed since start: 2h7m. |

| **Multi-frame KV cache: verified, no new bug** | 2026-09-09T22:15:00Z | 14,985,842 | Stage 4's last unverified item per `PORT_PLAN.md`: everything so far only proved the frame-0 empty-cache case. Captured two consecutive frames through the SAME `kv_cache` object (`capture_two_frames.py`, via the `tt-skyreels` venv), then ran our own `WaypointWorldModel.forward` twice on the same per-layer cache instances (frame 0 via `prefill_forward`, frame 1 via `decode_forward` -- no reset in between, matching the reference's persisted cache) and compared both against the reference's own two-call run. Frame 1's whole-model correlation (0.957) tracks frame 0's baseline (0.962) almost exactly (delta -0.004) -- a real bucket/slot indexing bug in the ring write/read logic would produce a much larger, structural degradation specific to frame 1, not a same-magnitude number. The ring-buffer write/read logic is correct across frames, not just for an empty buffer. Elapsed since start: 2h17m. |

| **Stage 5: VAE decoder hardware-verified** | 2026-09-09T22:35:00Z | 14,907,140 | **Milestone.** After a user steer on approach (asked directly whether to port straight to real `ttnn.conv2d`/`ttnn.upsample` or take a host-CPU-first correctness pass -- chose the former), ported the VAE decoder (`ChunkedStreamingTAEHV`, a TAEHV-family CNN autoencoder -- Conv2d/MemBlock/TGrow/Upsample, no attention) to real hardware ops, modeled on tt-metal's own SDXL VAE decoder (`models/demos/vision/generative/stable_diffusion/wormhole/tt/vae/`) for the calling convention rather than guessing the `ttnn.conv2d` API cold -- dispatched a research pass first to pull the exact signature, weight-prep idiom, and a working end-to-end example, which paid off: zero iteration cycles were spent on the API shape itself, only on two real hardware/config issues (`ttnn.open_mesh_device`'s `l1_small_size` defaults to 0 and conv2d's halo op cannot allocate at all with that; `ttnn.upsample` needs ROW_MAJOR input and rejects a tile-padded TILE tensor). Faithfully ported the reference's three-layer streaming contract (queue-based `_sequential_single_step`, the session-wide trim counter in `_streaming_decode_step`, and `decode()`'s first-call priming dance) -- caught and fixed my own first-draft simplification of this (treating the trim counter as per-call rather than session-wide) by testing it, not by re-reading the code more carefully after the fact. Verified against 3 consecutive real `decode()` calls (persisted streaming state, matching an actual session) on real Blackhole hardware: correlation >0.99 for all 12 output RGB frames, most >0.999. `encode()` (needed only once, to seed a session from a starting image) still pending. Elapsed since start: 2h37m. |

| **Stage 5 complete: VAE encoder hardware-verified too** | 2026-09-09T22:45:00Z | 14,865,951 | Ported `encode()` (needed once per session, to seed generation from a real starting image) the same way as the decoder: real `ttnn.conv2d` ops, same queue-based streaming machinery, reusing the already-verified `TTConv2d`/`MemBlockTT` classes rather than duplicating them, plus a new `TPoolTT` (temporal downscale: channel-concat 2 frames, 1x1 conv fuses them to 1). Hit one real bug: the spatial-size tracker used `layer_idx >= stride_conv_idx` to decide when H/W halves, which wrongly treated a strided conv's own INPUT as already-halved -- caught immediately by a real hardware allocator error (`MeshBuffer must be large enough to hold the tensor`), not a silent wrong-number result, and fixed by matching the decoder's own tracker's strict `>`. Verified against the reference's own `encode()` on the same input: correlation 0.9996. Stage 5 (VAE, both directions) is now fully hardware-verified. Elapsed since start: 2h47m. Repo now public at github.com/tsingletaryTT/tt-waypoint per Taylor's explicit request, with regular commits going forward; the HF push (under the `episod` account, also public) happens once the model reaches HF-ready status. |

| **Stage 6 wired, but a real quality limitation found in the denoising loop** | 2026-09-09T22:22:00Z | 14,872,270 | Wired the full interactive loop (`generation_loop.py`): `seed()` VAE-encodes a real image and commits it via one unfrozen forward call; `step()` draws noise, runs the real rectified-flow denoising (`x = x + dsigma*v`, read directly out of `WorldEngineDenoiseLoop._denoise_pass`/`_cache_pass`, not guessed), then one unfrozen commit. Found and fixed a real gap this exposed: `prefill_forward` hardcoded `is_frozen=False`, wrong for frame 0's own multi-step denoise loop (regression-tested against the existing hardware-verified suite -- no change in those numbers). Added `rope.py` for arbitrary-frame RoPE angles, verified bit-for-bit against captured reference tensors before trusting it. `test_generation_loop.py` (seeded with the SAME real image, injected with the SAME noise draws the reference used) showed the seed path is excellent (latent corr 0.9996, decoded-pixel corr ~0.96) but GENERATED frames are not: latent corr drops to 0.86-0.88, and decoded pixel correlation collapses to 0.05-0.24 -- a real quality problem, not just a number below a target. Root-caused rather than assumed: `test_frozen_step.py` isolated a SINGLE `is_frozen=True` forward call (the one new code path this loop exercises that no prior test touches) and found its correlation (0.953) matches the already-established single-call baseline, but with a real, systematic mean bias (computed -0.85 vs reference -0.16, roughly 0.4 std) that single-call tests using `is_frozen=False` never showed this starkly. The rectified-flow loop explicitly SUMS four such biased `v` outputs (`x = x + dsigma*v`, real addition, not a residual stream that dilutes noise) rather than passing them through 24 residual-connected layers, so the same per-call bf16 noise compounds differently -- additively, not just chained -- and the VAE decoder's `tanh`-based `Clamp` then reads a latent shifted into a different part of its saturation curve, amplifying a "0.86-correlated but biased" latent into badly wrong pixels. `ttnn`'s `scaled_dot_product_attention` only accepts bf16/bf8/bf4 (confirmed earlier during the VAE bring-up's own precision experiments), so full-fp32 attention isn't even available on this hardware to test as a mitigation -- this looks like a genuine, largely hardware-precision-constrained limitation on top of the already-documented Stage 4 finding, not a discrete logic bug still hiding somewhere. Flagged to Taylor rather than declaring Stage 6 done to the same bar as Stages 1-5. Elapsed since start: 2h24m (this row) / total session time significantly longer including two slow real-reference CPU captures at full 512x1024 resolution (~40min and ~8min). |

| **Chased and ruled out a false lead; original bf16-compounding explanation confirmed** | 2026-09-09T22:50:00Z | 14,790,416 | Taylor asked to keep investigating the generated-frame quality issue rather than accept it as a limitation. `test_sigma_sweep.py` isolated `_noise_conditioner` as PERFECT at every sigma (corr 0.999998) but found the full transformer output catastrophically wrong specifically at sigma=0.30078125 (corr 0.256, sandwiched between 0.95-0.96 at neighboring sigmas) -- looked like a real, discrete bug. Traced it further (`test_sigma_block_trace.py`, all 24 layers): the REFERENCE's own activations exploded ~12x (std 19->239) through blocks 4-23 specifically for that sigma, while ours stayed flat. Before concluding "real bug," reconsidered the test's own construction: it fed the SAME fixed random noise `x` across every sigma value tested, including 0.3 -- but in a real trajectory, x at sigma=0.3 is a PARTIALLY DENOISED signal from 2 prior steps, not raw noise. Labeling raw noise as "sigma=0.3" is out-of-distribution for the model and plausibly explains an explosive response in the reference itself, unrelated to any bug in this port. Verified this directly rather than just asserting it: extended `capture_generation_loop.py` to save real per-step (x_in, v_out) pairs from its own properly-evolved trajectory, then re-ran the same comparison (`test_step_trace.py`) using those real values instead of synthetic ones. Result: all 8 real denoising steps (both generated frames, all 4 sigmas each) show correlation 0.897-0.971 -- consistent with the already-established baseline, no catastrophic failure anywhere. The sigma-sweep "smoking gun" was confirmed to be an artifact of that test's own out-of-distribution construction, not a discovered bug. This restores and strengthens the original conclusion: generated-frame quality is limited by expected per-step bf16 noise (matching baseline), compounded by the rectified-flow loop's explicit summation across 4 steps (consistent with the 0.86-0.88 latent correlation seen in `test_generation_loop.py`), further amplified by the VAE decoder's saturating `tanh`-based `Clamp` into visibly bad pixels at the very low end of that latent correlation range. Elapsed since start: 2h52m (this row). |

*(Rows appended as work progresses. Elapsed time and token burn are computed against the
Start row.)*
